{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "MobileAuth": {
    // Replaces the list in appsettings.json, so it repeats the app and live web entries.
    "RedirectUris": [
      "ndeipichat://auth",
      "https://chat.ndeipi.com/signin/callback",
      "http://localhost:5057/signin/callback",
      "https://localhost:7257/signin/callback"
    ]
  }
}
